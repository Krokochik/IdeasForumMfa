<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.IdeasForumMfaLight" parent="android:Theme.Material.Light.NoActionBar">
        <!-- Primary brand color. -->
        <item name="android:colorBackground">@color/background</item>
        <item name="android:colorPrimaryDark">@color/primaryDark</item>
        <item name="android:textColorPrimary">@color/onPrimary</item>
        <item name="android:textColor">@color/primaryDark</item>
        <item name="android:colorPrimary">@color/primary</item>
        <item name="android:autofilledHighlight">@color/transparent</item>

        <!-- Secondary brand color. -->
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="Theme.IdeasForumMfaLight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.IdeasForumMfaLight.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.IdeasForumMfaLight.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>